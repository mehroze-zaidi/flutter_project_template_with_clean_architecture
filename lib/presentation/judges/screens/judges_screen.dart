import 'package:flutter/material.dart';

class JudgesScreen extends StatelessWidget {
  const JudgesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Judges Management Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}