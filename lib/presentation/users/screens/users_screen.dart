import 'package:flutter/material.dart';

class UserAdministrationsScreen extends StatelessWidget {
  const UserAdministrationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'User Administrations Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}